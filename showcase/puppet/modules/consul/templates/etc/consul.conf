{
  "data_dir":           "/var/consul",

  <% if @serverMode == true %>
    "bootstrap_expect": 1,
  <% else %>
    "bootstrap_expect": 0,
  <% end %>

  "server":             <%= @serverMode %>,
  "log_level":          "INFO",
  "enable_syslog":      true
}
